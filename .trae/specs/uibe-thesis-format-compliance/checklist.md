# Checklist

- [x] `\geometry{}` 参数已修改为上/下 3.6cm、左/右 3.1cm
- [x] 行间距已从 `\linespread{1.5}` 改为与格式文档一致的固定 22pt 设置
- [x] `\parindent{2em}` 首行缩进保留，段落两端对齐已确认
- [x] 英文摘要英文部分使用 Arial 字体
- [x] `\usepackage{graphicx}` 已添加，`\graphicspath{{figs/}}` 已设置
- [x] 正文中存在示例图片 `非线性构形状态转移过程示意图.jpg` 的 `\includegraphics` 引用
- [x] `\usepackage{array, booktabs}` 已添加，三线表样式可用
- [x] 正文中存在 demo 三线表
- [x] `lualatex` + `biber` 编译无错误
